services:
  - type: web
    name: teiko-immune-cell-analysis
    runtime: python
    plan: free
    buildCommand: make setup && make pipeline
    startCommand: make dashboard
    healthCheckPath: /api/health
    autoDeployTrigger: checksPass
